# mac 환경에 docker 설치

# 1. docker 설치
docker : cli + engine + desktop(gui) 가 합쳐진 프로그램
mac애서는 엔진 직접 실행이 아니라 데스크탑이 관리 -> cask 명령어로 설치
(리눅스랑은 다름)

- cask : mac의 gui 앱
```bash
brew install --cask docker
```